public class PasswordTest {
    /*File file = new File("C:\\Users\\Enes\\IdeaProjects\\OOP\\src\\dict.txt");

        if (file.createNewFile()) {
            System.out.println("File is created!");

        } else {
            System.out.println("File already exists.");
        }

        RandomAccessFile raf
                = new RandomAccessFile(file, "r");

        boolean invalid = false;

        String invalidPassword;
        String userPassword;

        Scanner scanner = new Scanner(System.in);
        System.out.println("Please enter a password: ");
        userPassword = scanner.nextLine();


        while (raf.getFilePointer() < raf.length() && !invalid) {

            invalidPassword = raf.readLine();

            if (invalidPassword.equals(userPassword)) {
                invalid = true;
                break;
            }
        }

        if (invalid) {

            System.out.println("Invalid Password! Please try another one");
            raf.close();

        }
    }

    public boolean isShort(String password) {

        return password.length() < 4;

    }

    public boolean isAcceptible(){
        return true;
    }
*/
}
